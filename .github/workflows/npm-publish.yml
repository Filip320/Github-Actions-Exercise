name: Mojot Prv DevOps Workflow

on:
  workflow_dispatch:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Chekor 1 - Kloniraj go kodot
        uses: actions/checkout@v4

      - name: Chekor 2 - Pozdrav od Ubuntu
        run: echo "Zdravo Filip! Ovaa poraka se pecati uspešno во виртуелната машина на GitHub!"

      - name: Chekor 3 - Proverka na sistemsko vreme
        run: date
