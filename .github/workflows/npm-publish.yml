name: Mojot Workflow

on:
  workflow_dispatch:
  push:
    branches: [ "main" ]

jobs:
  # 1. Главното облаче од кое тргнува сè
  build-and-test:
    name: build & test
    runs-on: ubuntu-latest
    steps:
      - name: Kloniraj go kodot
        uses: actions/checkout@v4
      - name: Izvrsi build
        run: echo "Pocetniot build e uspesen!"

  # 2. Прва гранка: summary (зависи САМО од build & test)
  summary:
    name: summary
    needs: build-and-test
    runs-on: ubuntu-latest
    steps:
      - name: Generiraj izvestaj
        run: echo "Generiranje na summary izvestaj..."

  # 3. Втора гранка: matrix compatibility (со матрица 18, 20, 22 спакувана во 1 job)
  matrix-compatibility:
    name: matrix compatibility
    needs: build-and-test
    runs-on: ubuntu-latest
    strategy:
      max-parallel: 1
      matrix:
        node-version: [18, 20, 22]
    steps:
      - name: Kloniraj go kodot
        uses: actions/checkout@v4

      - name: Postavi Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}

      - name: Proveri verzija
        run: node --version
